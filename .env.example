# Sanity CMS — add these after Phase 7 (Sanity setup)
PUBLIC_SANITY_PROJECT_ID=your_project_id_here
PUBLIC_SANITY_DATASET=production
# Read-only token for build-time GROQ queries (optional for public datasets)
SANITY_API_TOKEN=
