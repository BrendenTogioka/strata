---
interface Props {
  heroVideoUrl?: string
}
const { heroVideoUrl = '' } = Astro.props

// Scrub-friendly transformations: cap width 1600 + 30 fps so seek decodes are
// roughly half the work of the original 60 fps source, and VP9 (webm) scrubs
// more smoothly than H.264 in modern browsers.
const heroVideoWebm = heroVideoUrl
  ? heroVideoUrl.replace('/upload/', '/upload/vc_vp9,q_85,c_limit,w_1600,fps_30/').replace(/\.mp4$/, '.webm')
  : ''
const heroVideoMp4 = heroVideoUrl
  ? heroVideoUrl.replace('/upload/', '/upload/q_85,c_limit,w_1600,fps_30/')
  : ''
---

<section class="hero" id="hero">
  <div class="hero-img-wrap" id="hero-img-wrap">
    <img
      src="https://res.cloudinary.com/dlle6kl4n/video/upload/so_0/v1779938972/strata/videos/Sequence_04_6_iojrgo.jpg"
      alt="Strata — a field journal from the edges of the earth"
      fetchpriority="high"
      loading="eager"
      decoding="sync"
      class={heroVideoUrl ? 'hero-img-hidden' : ''}
    />
    {heroVideoUrl && (
      <video
        id="hero-video"
        class="hero-video"
        muted
        playsinline
        preload="auto"
        aria-hidden="true"
      >
        <source src={heroVideoWebm} type="video/webm" />
        <source src={heroVideoMp4}  type="video/mp4" />
      </video>
    )}
  </div>

  <div class="hero-overlay"></div>

  <div class="hero-content">
    <div class="hero-location">
      <div class="hero-location-dot"></div>
      <span>No Fixed Location — Always Moving</span>
    </div>

    <h1 class="hero-title" aria-label="Strata">
      <span class="line">STRA</span>
      <span class="line accent">TA</span>
    </h1>

    <div class="hero-sub">
      <p class="hero-sub-text">
        Adventures written in stone, fire, ice and root.<br />
        A field journal from the edges of the earth.
      </p>
      <div class="hero-scroll-cue">
        <span>Descend</span>
        <div class="scroll-bar"></div>
      </div>
    </div>
  </div>
</section>

